extends layouts/main.pug
include mixins/videoBlock

block content 
    .search__header
        h3 #{searchingBy}에 대한 검색 결과
    .search__videos 
        each item in videos
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })